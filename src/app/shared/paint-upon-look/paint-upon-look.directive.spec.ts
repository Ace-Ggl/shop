import { PaintUponLookDirective } from './paint-upon-look.directive';

describe('PaintUponLookDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintUponLookDirective();
    expect(directive).toBeTruthy();
  });
});
